# Folk CRM (contact form -> /api/contact)
# Create an API key in Folk: Settings -> Developers -> API keys
FOLK_API_KEY=

# Optional: Folk group ID to add every submission to (starts with grp_).
# Leave blank to create people without a group.
FOLK_GROUP_ID=
